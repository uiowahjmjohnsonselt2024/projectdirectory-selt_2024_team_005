<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Shards Of The Grid With Saas Principles" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" cogit ntent="yes">
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'grids', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_importmap_tags %>
  </head>

  <body >
  <% unless (controller_name == 'sessions' && action_name == 'new') ||
    (controller_name == 'password_resets' && (action_name == 'new' || action_name == 'create' || action_name == 'edit') ||
    (controller_name == 'users' && (action_name == 'create' || action_name == 'new' || action_name == 'edit')) ) %>
    <header>
      <h1 class="main_header">Group 5 Game</h1>
    </header>
  <% end %>
  <% unless (controller_name == 'sessions' && action_name == 'new') ||
    (controller_name == 'password_resets' && (action_name == 'new' || action_name == 'create' || action_name == 'edit') ||
      (controller_name == 'users' && (action_name == 'create' || action_name == 'new' || action_name == 'edit'))) %>
    <nav class="navbar">
      <ul class="nav-links">
        <li><%= link_to 'Profile', user_path(current_user.username) %></li>
        <li><%= link_to 'Store', store_path(current_user.username) %></li>
        <li><%= link_to 'Grid', grids_path %></li>
        <%= button_to 'Logout', logout_path, method: :delete, class: 'btn btn-primary'%>

      </ul>
    </nav>
  <% end %>

  <% if flash[:alert] %>
    <div ><%= flash[:alert] %></div>
  <%end %>
  <% if flash[:notice] %>
    <div ><%= flash[:notice] %></div>
  <% end %>

  <%= yield %>
  </body>
</html>

<!-- Chat box for displaying messages -->
<div id="chat-box" style="height: 300px; overflow-y: auto; border: 1px solid #ccc; padding: 10px;">
  <!-- Messages will be appended here dynamically -->
</div>

<div id="chat-container">
  <!-- Displaying the chat messages -->
  <div id="message-list"></div>

  <!-- Input for sending new messages -->
  <input type="text" id="message-input" placeholder="Type your message...">
  <button id="send-button">Send</button>
</div>
